@page "/cities/edit/{CityId:int}"

<h3>Editar Provincia</h3>

@if (city is null)
{
    <Loading />
}
else
{
    <FormWithName @ref="cityForm" Label="Provincia:" Model="city" OnValidSubmit="SaveAsync" ReturnAction="Return" />
}
